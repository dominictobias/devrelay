{
  "name": "@devrelay/{{platform}}-{{arch}}",
  "version": "{{version}}",
  "description": "devrelay binary for {{platform}}/{{arch}}",
  "license": "MIT",
  "os": ["{{platform}}"],
  "cpu": ["{{arch}}"],
  "bin": {
    "devrelay": "bin/devrelay"
  }
}
